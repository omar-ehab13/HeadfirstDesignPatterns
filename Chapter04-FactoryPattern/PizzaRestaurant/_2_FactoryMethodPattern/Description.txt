# The Factory Method Pattern
	defines an interface for creating an object, but lets subclasses to decide which class to instantiate.
	Factory Method lets a class defer instantiation to subclass.

# Dependency Inversion Priniciple
	Depend upon abstractions. Do not depent upon concrete classes.